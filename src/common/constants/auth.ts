/**
 * Cookie name for storing the refresh token used for token refresh operations.
 */
export const AUTH_REFRESH_TOKEN_NAME = 'refresh_token';

/**
 * Cookie name for storing the signed user ID for session management.
 */
export const AUTH_USER_ID_NAME = 'user_id';

/**
 * Cookie name for storing the signed session token for authentication state.
 */
export const AUTH_SESSION_NAME = 'session_token';
